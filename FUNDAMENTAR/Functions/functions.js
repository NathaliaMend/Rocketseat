// É possível declarar funções dentro de variáveis
// function expression
// function anonymous
// https://playcode.io/1097684

// parâmetros (parameters) da função / serve para receber os valores
const sum = function(number1, number2) {
    console.log(number1 + number2)
}

sum(2, 3) // 2 e 3 são arguments (argumentos) de uma função executada, porém função CRIADA são parâmetros.

// RESUME : Aprendi a passar argumentos para a invocação de uma função e a criar os parâmetros que vão receber os argumentos da função.

// reutilizando o bloco de códigos

sum(4, 5)
sum(56, 3587245029)